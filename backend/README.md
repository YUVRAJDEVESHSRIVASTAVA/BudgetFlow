# Backend

This folder contains the Django REST API for ExpenseFlow.

## Commands

```powershell
python manage.py makemigrations
python manage.py migrate
python manage.py runserver
```

The API is available under `/api/`, and the browser UI is served from the root routes.
